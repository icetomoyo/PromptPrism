// PromptPrism CLI — stub. Real CLI lands in W2 (PROJECT_PLAN.md).
// Until then, exporting an empty namespace keeps the package buildable
// so contributors can `npm install` cleanly.

export const placeholder = true;
